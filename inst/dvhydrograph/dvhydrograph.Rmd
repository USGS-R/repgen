![](http://cida.usgs.gov/images/usgslogo.jpg)

## `r data[['title']]`  

**Location:**  `r data[['reportMetadata']][['stationId']]` - `r data[['reportMetadata']][['stationName']]`

**Period:**  `r as.Date(data[['reportMetadata']][['startDate']])` to `r as.Date(data[['reportMetadata']][['endDate']])`

**First Down Chain Time Series:** `r data[['reportMetadata']][['downChainDescriptions1']]`

**Second Down Chain Time Series:** `r data[['reportMetadata']][['downChainDescriptions2']]`

**Third Down Chain Time Series:** `r data[['reportMetadata']][['downChainDescriptions3']]`

`r if (length(author) != 0) { paste("**Author:**",author[[1]]) }`

`r if (length(author) == 0) { paste("**Author:** Username") }`

**Created:**  `r Sys.time()`

***

```{r echo=FALSE, warning=FALSE, message=FALSE, results='asis', dev='svg', fig.height=10, fig.width=13}

#dvhydrographPlot(data)
allPlots <- dvhydrographPlot(data)
for (i in seq_len(length(allPlots))) {

  x <- allPlots[[i]]
  par(mar=c(13, 3, 4, 2))
  print(allPlots$dvhydro)
  cat("\n\n")
  print(allPlots$secRefPlot)
  cat("\n\n")
  print(allPlots$terRefPlot)
  cat("\n\n")
  print(allPlots$quaRefPlot)
  cat("\n\n")
}
```

`r data[['simsUrl']]`

`r data[['waterdataUrl']]`
